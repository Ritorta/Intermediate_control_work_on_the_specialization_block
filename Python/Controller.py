def main():
    while True:
        num = view.user_input
        if num == "1":
            noteapp.add_note(title, body)
        else:
            print('Error: Invalid number, choice number is menu.')